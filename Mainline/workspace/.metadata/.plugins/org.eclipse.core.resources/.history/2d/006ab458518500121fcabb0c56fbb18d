package com.example.client;


import java.net.InetAddress;
import java.net.Socket;

import android.os.Bundle;
import android.app.Activity;
import android.view.Menu;
import android.widget.Toast;

public class ClientActivity extends Activity {

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_client);
		
		Toast.makeText(getApplicationContext(), "APP STARTED", Toast.LENGTH_SHORT).show();
		
		Thread thread = new Thread()
		{
		    @Override
		    public void run() {
		    	Toast.makeText(getBaseContext(), "THREAD STARTED", Toast.LENGTH_SHORT).show();
		    	try {
					Socket test = new Socket(InetAddress.getByName("10.0.1.3"),6881);
					ClientActivity.sayGood();
					test.getReceiveBufferSize();
				
				} catch (Exception e) {
					e.printStackTrace();
				}
		    }
		};

		thread.start(); 
		
		
		
	}
	
	public static void sayGood(){
		Toast.makeText(this, "WORKED", Toast.LENGTH_SHORT).show();

	}
	
	public void sayBad(){
		Toast.makeText(getBaseContext(), "NOT CONNECTED BIOTCH", Toast.LENGTH_SHORT).show();

	}

	@Override
	public boolean onCreateOptionsMenu(Menu menu) {
		// Inflate the menu; this adds items to the action bar if it is present.
		getMenuInflater().inflate(R.menu.client, menu);
		return true;
	}

}
